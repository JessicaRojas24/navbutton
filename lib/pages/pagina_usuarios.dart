import 'package:flutter/material.dart';

class PaginaUsuarios extends StatelessWidget {
  const PaginaUsuarios({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("pagina de usuarios"),
      ),
    );
  }
}